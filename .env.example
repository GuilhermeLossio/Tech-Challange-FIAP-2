# AWS / storage
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=your-raw-bucket
S3_BUCKET=your-raw-bucket
RAW_PREFIX=raw

# Ingestion
TICKERS=GOLL4,AZUL4,EMBR3,EVEB31

# API auth (optional)
CRON_SECRET=change-me

# Glue trigger lambda
GLUE_JOB_NAME=your-glue-job-name

# Legacy lambda flow (only if using lambda/lambda_functionpy)
RAW_BUCKET=your-raw-bucket
